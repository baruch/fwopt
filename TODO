+ Version 0.1
  c Read input from file
    # Reads input from stdin, good enough for now
  + Implement test suite
    + Test drop rule between accept rules
    + Test multiple interfaces being grouped into different groups
  + Fix bugs detected in the test suite
  + Proof of concept is ready

* Version 0.2
  + Read "jump to chain" rules
  + Implement -V/--version
  + Inline such chains into one big initial group
  - Group rules on
    + Output interface
    - source ip
    - dest ip
    - protocol
    - source port
    - dest port
	+ flow state tracking
